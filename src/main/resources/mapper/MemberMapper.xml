<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.example.springExsecurity.Repository.MemberMapper">
    <insert id="saveMember" parameterType="member">
       insert into tb_member_Security (
          id,
          password,
          name
       ) values (
          #{id},
          #{password},
          #{name}
       )
      </insert>
      <select id="findMember" parameterType="string" resultType="member">
      select * From tb_member_security where id=#{id} 
      </select>
  </mapper>